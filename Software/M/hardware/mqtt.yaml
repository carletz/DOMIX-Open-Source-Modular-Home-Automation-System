mqtt:
  broker: ${mqtt_broker}          ## just for example, change at your use
  port: ${mqtt_port}
  username: ${mqtt_username}
  password: ${mqtt_password}
  discovery: true
  birth_message:
    topic: ${device_name}/state
    payload: online
  will_message:
    topic: ${device_name}/state
    payload: offline